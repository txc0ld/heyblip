# PM secrets template. Copy to ~/heyblip/.claude/skills/secrets/.env and populate.
# That target path is gitignored. This template is committed but contains no real values.

export NOTION_TOKEN="ntn_REPLACE_ME"
export SLACK_BOT_TOKEN="xoxb-REPLACE-ME"
export GITHUB_PAT="ghp_REPLACE_ME"
